const mutations = {
    setResponse: (state, payload) => {
        state.response = payload;
    },
    setUser: (state, payload) => {
        state.user = payload;
    },
};

export default mutations;
